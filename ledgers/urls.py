from django.urls import path
from .views import *

app_name = 'ledgers'

urlpatterns = [
    # path('fill/', LedgerEntryCreateView.as_view, name='legerCreate'),
    # path('ledgers/fill/<int:ledger_id>/', // 가계부 수정
]